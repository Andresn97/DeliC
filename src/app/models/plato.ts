import { Local } from "./local";

export class Plato {
  local: Local;
  nombre: string;
  precio: string;
  ingredientes: string;
  descripcion: string;
  calificacion: number;
  fechaRegistro: any;
  activo: boolean;
}
